# Relay briefs

Write one reviewed brief per page, flow or exploration, starting from the templates in [`docs/ia/templates/`](../../../../docs/ia/templates/). A brief records intent; the generated Figma nodes and the signature baselines record output, and neither replaces the other. A design-system proposal lives with its design system (for Primer, under `design-systems/primer/docs/`).

## Pages

| Brief | State |
| --- | --- |
| [Relay](./relay.md) | The example app shipped with the repository |

## Active explorations

None. The Design lab is empty.
