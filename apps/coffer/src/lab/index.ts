/* Design lab experiments, registered only while they are under review. An
 * experiment leaves once its decision and evidence have moved to the docs. */

import type { LabExperiment } from '@figma-harness/contract';

export const LAB_EXPERIMENTS: readonly LabExperiment[] = Object.freeze([]);
